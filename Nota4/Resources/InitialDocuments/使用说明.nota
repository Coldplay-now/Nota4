---
title: 使用说明
created: 2024-01-01T00:00:00Z
updated: 2025-11-20T09:01:00Z
starred: true
pinned: true
tags: [帮助, 文档]
checksum: ""
---

# 使用说明

欢迎使用 Nota4，一款专为写作和笔记管理设计的现代化 macOS 应用。

![Nota4 Logo](icon_1024x1024.png)

> 💡 **提示**：点击右上角的 👁️ 大眼睛图标，可以切换到预览模式，以更美观的方式阅读本文档。

[TOC]

---

## 1. 快速开始

### 1.1. 创建笔记

点击左上角的"+"按钮，或使用快捷键 `⌘N` 创建新笔记。每个笔记都会自动保存为 `.nota` 格式文件。

**示例代码**：

```swift
// 创建新笔记
let newNote = Note(
    noteId: UUID().uuidString,
    title: "我的第一个笔记",
    content: "# 欢迎\n\n这是你的第一个笔记。"
)
```

### 1.2. 编辑笔记

Nota4 支持完整的 Markdown 语法，包括：

- **实时预览**：点击右上角的 👁️ 大眼睛图标切换到预览模式，查看渲染效果
- **自动保存**：停止输入后自动保存
- **语法高亮**：代码块支持多种编程语言
- **工具栏**：使用编辑器顶部的工具栏快速插入 Markdown 格式

**工具栏功能说明**：
- **格式按钮**：加粗（⌘B）、斜体（⌘I）、行内代码（⌘E）
- **标题菜单**：快速插入标题 1-6（⌘⌥1-6）
- **列表按钮**：无序列表（⌘L）、有序列表（⌘⌥L）、任务列表（⌘⌥⇧L）
- **插入按钮**：链接（⌘K）、代码块（⇧⌘K）、跨行代码块（⌘⌥K）
- **视图切换**：编辑模式、预览模式、分屏模式

**编辑流程**：

```mermaid
graph LR
    A[开始编辑] --> B[输入内容]
    B --> C{停止输入?}
    C -->|是| D[自动保存]
    C -->|否| B
    D --> E[更新数据库]
    E --> F[完成]
```

### 1.3. 管理笔记

#### 星标笔记

右键点击笔记选择"星标"或使用工具栏按钮。星标的笔记会在侧边栏"星标"分类中显示。

#### 置顶笔记

右键点击笔记选择"置顶"或使用工具栏按钮。置顶的笔记会始终显示在列表顶部。

**数学公式示例**：

笔记的优先级可以用以下公式表示：

$$优先级 = \begin{cases}
置顶 & \text{如果 } isPinned = true \\
星标 & \text{如果 } isStarred = true \\
普通 & \text{其他情况}
\end{cases}$$

#### 删除笔记

删除的笔记会移动到"已删除"分类，可以在那里恢复或永久删除。

## 2. 核心功能

### 2.1. 搜索功能

点击搜索图标或使用快捷键 `⌘F` 打开搜索。支持标题和内容的全文搜索。

**搜索算法**：

```python
def search_notes(query, notes):
    """搜索笔记"""
    keywords = query.lower().split()
    results = []
    for note in notes:
        title_match = any(kw in note.title.lower() for kw in keywords)
        content_match = any(kw in note.content.lower() for kw in keywords)
        if title_match or content_match:
            results.append(note)
    return results
```

### 2.2. 批量操作

按住 `⌘` 键可以多选笔记，然后进行批量操作：

- 批量星标/取消星标
- 批量置顶/取消置顶
- 批量删除
- 批量导出

### 2.3. 导出功能

支持多种导出格式：

| 格式 | 扩展名 | 说明 |
|------|--------|------|
| Nota | .nota | 原生格式，包含元数据 |
| Markdown | .md | 标准 Markdown 格式 |
| HTML | .html | 网页格式 |
| PDF | .pdf | 便携文档格式 |

## 3. 工具栏和首选项

### 3.1. 编辑器工具栏

编辑器顶部提供了丰富的 Markdown 格式化工具，让您快速插入常用格式：

![工具栏](toolbar.jpeg)

**工具栏按钮说明**：

| 按钮组 | 功能 | 快捷键 |
|--------|------|--------|
| **格式组** | | |
| 加粗 | 文本加粗 | `⌘B` |
| 斜体 | 文本斜体 | `⌘I` |
| 行内代码 | 插入行内代码 | `⌘E` |
| **标题菜单** | | |
| 标题 1-6 | 插入标题 | `⌘⌥1-6` |
| **列表组** | | |
| 无序列表 | 插入无序列表 | `⌘L` |
| 有序列表 | 插入有序列表 | `⌘⌥L` |
| 任务列表 | 插入任务列表 | `⌘⌥⇧L` |
| **插入组** | | |
| 链接 | 插入链接 | `⌘K` |
| 代码块 | 插入代码块 | `⇧⌘K` |
| 跨行代码块 | 插入带语言的代码块 | `⌘⌥K` |
| 区块引用 | 插入引用块 | `⇧⌘Q` |
| 分隔线 | 插入分隔线 | `⇧⌘-` |
| **视图切换** | | |
| 编辑模式 | 仅显示编辑器 | - |
| 预览模式 | 仅显示预览 | - |
| 分屏模式 | 同时显示编辑和预览 | - |

### 3.2. 首选项设置

点击菜单栏 **Nota4 > 首选项...** 或使用快捷键 `⌘,` 打开首选项窗口，可以自定义应用的各种设置。

![首选项 - 通用设置](preference.jpeg)

**首选项功能**：

1. **编辑器设置**
   - 字体和字号：自定义编辑器字体
   - 行高：调整行间距
   - 自动保存：设置自动保存间隔
   - 显示行号：是否显示行号

2. **预览设置**
   - 主题选择：选择预览主题（浅色/深色/GitHub/Notion 等）
   - 代码高亮：选择代码高亮主题
   - 数学公式：启用/禁用 LaTeX 数学公式渲染
   - Mermaid 图表：启用/禁用 Mermaid 图表渲染

![首选项 - 高级设置](preference2.jpeg)

3. **外观设置**
   - 布局模式：三栏/两栏/一栏布局
   - 侧边栏宽度：调整侧边栏宽度
   - 笔记列表宽度：调整笔记列表宽度

4. **数据设置**
   - 存储位置：查看笔记存储位置
   - 备份设置：配置自动备份
   - 导入/导出：管理导入导出选项

## 4. 快捷键参考

### 4.1. 文件操作

| 快捷键 | 功能 |
|--------|------|
| `⌘N` | 创建新笔记 |
| `⌘O` | 打开文件 |
| `⌘S` | 保存笔记 |
| `⇧⌘I` | 导入笔记 |
| `⇧⌘E` | 导出笔记 |
| `⌘W` | 关闭窗口 |

### 4.2. 编辑操作

| 快捷键 | 功能 |
|--------|------|
| `⌘Z` | 撤销 |
| `⇧⌘Z` | 重做 |
| `⌘X` | 剪切 |
| `⌘C` | 复制 |
| `⌘V` | 粘贴 |
| `⌘A` | 全选 |
| `⌘F` | 查找 |
| `⌘⌥F` | 查找并替换 |
| `⌘G` | 查找下一个 |
| `⇧⌘G` | 查找上一个 |

### 4.3. 格式操作

| 快捷键 | 功能 |
|--------|------|
| `⌘B` | 加粗 |
| `⌘I` | 斜体 |
| `⇧⌘U` | 下划线 |
| `⇧⌘X` | 删除线 |
| `⌘⌥1-6` | 标题 1-6 |
| `⌘L` | 无序列表 |
| `⌘⌥L` | 有序列表 |
| `⌘⌥⇧L` | 任务列表 |
| `⌘K` | 插入链接 |
| `⇧⌘K` | 代码块 |
| `⌘⌥K` | 跨行代码块 |
| `⌘E` | 行内代码 |
| `⇧⌘Q` | 区块引用 |
| `⇧⌘-` | 分隔线 |
| `⇧⌘A` | 插入附件 |

### 4.4. 笔记操作

| 快捷键 | 功能 |
|--------|------|
| `⌘D` | 星标/取消星标 |
| `⇧⌘P` | 置顶/取消置顶 |
| `⌘⌫` | 移至废纸篓 |
| `⇧⌘⌫` | 永久删除 |

### 4.5. 视图操作

| 快捷键 | 功能 |
|--------|------|
| `⇧⌘1` | 三栏布局 |
| `⇧⌘2` | 两栏布局 |
| `⇧⌘3` | 一栏布局 |
| `⇧⌘L` | 切换布局模式 |
| `⌃⌘F` | 进入全屏 |
| `⌘M` | 最小化窗口 |

### 4.6. 其他操作

| 快捷键 | 功能 |
|--------|------|
| `⌘,` | 打开首选项 |
| `⌘?` | 显示帮助 |

## 5. 文件管理

### 5.1. 存储位置

笔记默认存储在：

```
~/Library/Containers/com.nota4.Nota4/Data/Documents/NotaLibrary/
```

### 5.2. 文件格式

每个笔记都是一个 `.nota` 文件，包含：

- **YAML 格式的元数据**：标题、时间、标签等
- **Markdown 格式的正文内容**

**文件结构**：

```yaml
---
title: 笔记标题
created: 2024-01-01T00:00:00Z
updated: 2024-01-01T00:00:00Z
starred: true
pinned: true
tags: [标签1, 标签2]
---

# Markdown 内容
```

## 6. 主题和外观

### 6.1. 预览主题

在首选项中可以选择预览主题，支持：

1. **Light** - 浅色主题
2. **Dark** - 深色主题
3. **GitHub** - GitHub 风格
4. **Notion** - Notion 风格
5. **其他内置主题**

### 6.2. 编辑器外观

可以在首选项中自定义：
- 编辑器字体和字号
- 行高和行间距
- 是否显示行号
- 编辑器背景色

## 7. 常见问题

### 6.1. 数据安全

- **自动备份**：每日自动备份数据库
- **版本控制**：支持笔记版本历史（开发中）
- **本地存储**：所有数据存储在本地，不会上传到云端

### 6.2. 性能优化

- **延迟加载**：笔记列表采用分页加载
- **索引优化**：数据库使用索引加速查询
- **缓存机制**：预览内容会缓存以提高性能

**性能指标**：

$$性能分数 = \frac{加载速度 \times 响应时间}{内存占用 \times CPU使用率}$$

## 8. 技术支持

### 获取帮助

- **GitHub 仓库**：[https://github.com/Coldplay-now/Nota4](https://github.com/Coldplay-now/Nota4)
- **问题反馈**：[https://github.com/Coldplay-now/Nota4/issues](https://github.com/Coldplay-now/Nota4/issues)
- **文档中心**：查看 `Markdown完整示例` 文档了解更多 Markdown 功能
- **应用内帮助**：点击菜单栏 **帮助 > Nota4 帮助** 或使用快捷键 `⌘?`

### 版本信息

当前版本：v1.2.7

**更新日志**：

```mermaid
gantt
    title Nota4 开发时间线
    dateFormat  YYYY-MM-DD
    
    section v1.0
    基础功能开发       :a1, 2024-01-01, 30d
    
    section v1.1
    功能增强          :a2, after a1, 20d
    性能优化          :a3, after a2, 10d
    
    section v1.2
    高级功能          :b1, after a3, 30d
```

---

**感谢使用 Nota4！**

如有任何问题或建议，欢迎通过 GitHub Issues 反馈。
