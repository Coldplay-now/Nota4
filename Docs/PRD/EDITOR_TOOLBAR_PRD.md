# Nota4 编辑器工具栏 PRD

> **版本**: v1.0.0  
> **创建日期**: 2025-11-16  
> **文档状态**: 设计中 🚧  
> **架构**: SwiftUI 4.0 + TCA  

---

## 📋 目录

1. [产品概述](#1-产品概述)
2. [设计原则](#2-设计原则)
3. [功能需求](#3-功能需求)
4. [技术架构](#4-技术架构)
5. [交互设计](#5-交互设计)
6. [视觉设计](#6-视觉设计)
7. [响应式设计](#7-响应式设计)
8. [开发计划](#8-开发计划)
9. [测试计划](#9-测试计划)

---

## 1. 产品概述

### 1.1 背景与问题

**现状**：
- ✅ Nota4 已实现 Markdown 格式化快捷键（⌘B、⌘I 等）
- ✅ 已实现完整的 Action 层（formatBold, insertHeading 等）
- ❌ SwiftUI TextEditor 不支持自定义右键菜单
- ❌ 用户无法直观发现可用的 Markdown 格式功能

**用户痛点**：
1. **可发现性差**：用户不知道有哪些格式化功能可用
2. **学习成本高**：需要记忆大量快捷键
3. **操作门槛**：新手用户不熟悉 Markdown 语法

### 1.2 解决方案

在编辑器顶部提供一个 **Markdown 格式工具栏**，特点：
- 🎯 **可视化操作**：点击按钮即可插入格式
- 🔍 **可发现性强**：所有功能一目了然
- ⚡ **保留快捷键**：高级用户仍可使用快捷键
- 🏗️ **原生体验**：充分利用 SwiftUI 4.0 和 TCA 特性

### 1.3 目标用户

| 用户类型 | 使用方式 | 占比 |
|---------|---------|------|
| **新手用户** | 主要使用工具栏按钮 | 40% |
| **中级用户** | 工具栏 + 快捷键混用 | 35% |
| **高级用户** | 主要使用快捷键 | 25% |

### 1.4 核心价值

- ✅ **降低使用门槛**：新手用户无需学习 Markdown 语法即可使用
- ✅ **提升效率**：减少记忆负担，点击即用
- ✅ **增强可发现性**：用户能够探索和学习新功能
- ✅ **保持一致性**：与其他现代 Markdown 编辑器（Typora、Notion）对齐

---

## 2. 设计原则

### 2.1 SwiftUI 4.0 优先原则

#### 原则 1: 使用原生组件，而非自定义实现

**✅ DO**：
```swift
// 使用原生 Toolbar API
.toolbar {
    ToolbarItemGroup(placement: .automatic) {
        // 工具栏按钮
    }
}
```

**❌ DON'T**：
```swift
// 自定义浮动工具栏（复杂且不符合系统规范）
ZStack {
    TextEditor(...)
    VStack {
        CustomToolbar()  // ❌ 不推荐
        Spacer()
    }
}
```

**理由**：
- 原生 Toolbar 自动适配 macOS 标准位置
- 自动处理窗口大小变化
- 系统级动画和交互
- 更好的辅助功能支持

---

#### 原则 2: 声明式优于命令式

**✅ DO**：
```swift
// 声明式：描述"是什么"
Button("加粗", systemImage: "bold") {
    store.send(.formatBold)
}
.disabled(!store.hasNote)  // 状态驱动
```

**❌ DON'T**：
```swift
// 命令式：描述"怎么做"
if store.hasNote {
    createButton(title: "加粗", action: formatBold)
} else {
    createDisabledButton(title: "加粗")
}
```

**理由**：
- 代码更简洁、可读性强
- SwiftUI 自动优化性能
- 状态变化时自动更新 UI

---

#### 原则 3: 利用 SF Symbols 4.0 变体系统

**✅ DO**：
```swift
Label("加粗", systemImage: "bold")
    .symbolVariant(isBoldActive ? .fill : .none)  // 自动切换变体
    .symbolRenderingMode(.hierarchical)  // 分层渲染
```

**❌ DON'T**：
```swift
// 手动切换图标
Image(systemName: isBoldActive ? "bold.fill" : "bold")  // ❌ 不推荐
```

**理由**：
- 自动支持不同状态（.fill, .slash, .circle）
- 流畅的动画过渡
- 自动适配 Light/Dark Mode

---

#### 原则 4: 响应式布局自动适配

**✅ DO**：
```swift
ViewThatFits {
    // 宽屏：显示所有按钮
    fullToolbar
    // 窄屏：部分收起
    compactToolbar
}
```

**❌ DON'T**：
```swift
// 手动监听窗口大小
@State private var windowWidth: CGFloat = 0
if windowWidth > 800 {
    fullToolbar
} else {
    compactToolbar
}
```

**理由**：
- 自动根据可用空间选择最佳布局
- 无需手动监听几何变化
- 性能更优

---

### 2.2 TCA 架构原则

#### 原则 1: 单一数据源（Single Source of Truth）

```swift
// ✅ 工具栏状态由 EditorFeature.State 驱动
struct EditorFeature {
    struct State {
        var note: Note?
        var content: String
        
        // 计算属性：工具栏按钮状态
        var hasNote: Bool { note != nil }
        var canFormat: Bool { !content.isEmpty }
    }
}

// ✅ 工具栏 View 读取 Store
struct MarkdownToolbar: View {
    let store: StoreOf<EditorFeature>
    
    var body: some View {
        Button("加粗") { store.send(.formatBold) }
            .disabled(!store.hasNote)  // 由 State 驱动
    }
}
```

**理由**：
- 状态集中管理，易于调试
- UI 自动响应状态变化
- 避免状态不一致

---

#### 原则 2: Action 驱动的交互

```swift
// ✅ 所有交互通过 Action 触发
enum Action {
    case formatBold
    case formatItalic
    case insertHeading(level: Int)
}

// ✅ Reducer 处理业务逻辑
func reduce(into state: inout State, action: Action) -> Effect<Action> {
    switch action {
    case .formatBold:
        return .send(.insertMarkdown(.bold))
    // ...
    }
}
```

**理由**：
- 业务逻辑与 UI 分离
- 易于测试（TestStore）
- 操作可追溯、可回放

---

#### 原则 3: 可组合的 Reducer

```swift
// ✅ 工具栏按钮可复用 EditorFeature 的 Action
struct MarkdownToolbar: View {
    let store: StoreOf<EditorFeature>  // 复用 EditorFeature
    
    var body: some View {
        Button("加粗") {
            store.send(.formatBold)  // 调用已有 Action
        }
    }
}
```

**理由**：
- 无需创建新的 Feature
- 复用已有业务逻辑
- 保持架构简洁

---

### 2.3 用户体验原则

#### 原则 1: 渐进式披露（Progressive Disclosure）

- **初级用户**：只看到最常用的 5-7 个按钮
- **中级用户**：展开"更多"菜单查看完整功能
- **高级用户**：使用快捷键，工具栏可隐藏

#### 原则 2: 即时反馈（Immediate Feedback）

- 点击按钮 → 立即插入 Markdown 语法
- Hover 显示工具提示（功能名 + 快捷键）
- 按钮状态实时更新（启用/禁用）

#### 原则 3: 容错性（Error Prevention）

- 禁用不可用的按钮（如：无笔记时禁用星标）
- 清晰的视觉反馈（灰色 = 不可用）
- 工具提示说明禁用原因

---

## 3. 功能需求

### 3.1 功能清单

| 分组 | 功能 | 图标 | 快捷键 | 优先级 | 说明 |
|-----|------|------|--------|--------|------|
| **格式** | 加粗 | `bold` | ⌘B | P0 | 插入 `****` |
| | 斜体 | `italic` | ⌘I | P0 | 插入 `**` |
| | 行内代码 | `chevron.left.forwardslash.chevron.right` | ⌘E | P0 | 插入 `` `` `` |
| **标题** | 标题菜单 | `textformat.size` | - | P0 | 下拉菜单 |
| | ↳ 标题 1 | `h1.square` | ⌘1 | P0 | 插入 `# ` |
| | ↳ 标题 2 | `h2.square` | ⌘2 | P0 | 插入 `## ` |
| | ↳ 标题 3 | `h3.square` | ⌘3 | P0 | 插入 `### ` |
| **列表** | 无序列表 | `list.bullet` | ⌘L | P0 | 插入 `- ` |
| | 有序列表 | `list.number` | ⇧⌘L | P0 | 插入 `1. ` |
| | 任务列表 | `checklist` | ⌥⌘L | P1 | 插入 `- [ ] ` |
| **插入** | 链接 | `link` | ⌘K | P0 | 插入 `[]()` |
| | 代码块 | `curlybraces` | ⇧⌘K | P0 | 插入 `` ```\n\n``` `` |
| **笔记** | 切换星标 | `star` / `star.fill` | ⌘D | P1 | 切换星标状态 |

**总计**：12 个功能（P0: 10 个，P1: 2 个）

---

### 3.2 功能优先级

#### P0 - 核心功能（必须实现）⭐

**格式化**（3 个）：
- 加粗、斜体、行内代码

**标题**（3 个）：
- 标题 1/2/3（通过下拉菜单）

**列表**（2 个）：
- 无序列表、有序列表

**插入**（2 个）：
- 链接、代码块

**小计**：10 个

---

#### P1 - 重要功能（优先实现）

**列表**（1 个）：
- 任务列表

**笔记操作**（1 个）：
- 切换星标

**小计**：2 个

---

#### P2 - 扩展功能（延后实现）

- 删除线（⌥⌘S）
- 引用块（⌘'）
- 分隔线（⌘-）
- 表格（⌘T）
- 图片插入（⌘G）
- 数学公式（⌘M）

---

### 3.3 响应式功能

| 窗口宽度 | 显示模式 | 说明 |
|---------|---------|------|
| **> 1000px** | 完整模式 | 显示所有按钮（12 个） |
| **800-1000px** | 标准模式 | 显示核心按钮（10 个）+ "更多"菜单 |
| **600-800px** | 紧凑模式 | 显示最常用（6 个）+ "更多"菜单 |
| **< 600px** | 最小模式 | "格式"菜单（所有功能收起） |

---

## 4. 技术架构

### 4.1 架构层次

```
┌─────────────────────────────────────────────┐
│  View Layer (SwiftUI)                       │
│  ┌──────────────────────────────────────┐  │
│  │  NoteEditorView                      │  │
│  │    └─ .toolbar {                     │  │
│  │         MarkdownToolbar(store)       │  │
│  │       }                               │  │
│  └──────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
                    ↕ (读取 State / 发送 Action)
┌─────────────────────────────────────────────┐
│  State Management (TCA)                     │
│  ┌──────────────────────────────────────┐  │
│  │  EditorFeature                       │  │
│  │    ├─ State (数据源)                 │  │
│  │    ├─ Action (用户交互)              │  │
│  │    └─ Reducer (业务逻辑)             │  │
│  └──────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
                    ↕ (调用已有 Action)
┌─────────────────────────────────────────────┐
│  Business Logic Layer                       │
│  ┌──────────────────────────────────────┐  │
│  │  formatBold → insertMarkdown(.bold)  │  │
│  │  insertHeading1 → ...                │  │
│  └──────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

---

### 4.2 组件结构

```swift
// 主视图：NoteEditorView
struct NoteEditorView: View {
    @Bindable var store: StoreOf<EditorFeature>
    
    var body: some View {
        TextEditor(text: $store.content)
            .toolbar {
                ToolbarItemGroup(placement: .automatic) {
                    MarkdownToolbar(store: store)  // 工具栏组件
                }
            }
    }
}

// 工具栏组件：MarkdownToolbar
struct MarkdownToolbar: View {
    let store: StoreOf<EditorFeature>
    
    var body: some View {
        ViewThatFits {
            fullToolbar   // 完整模式
            compactToolbar  // 紧凑模式
        }
    }
    
    private var fullToolbar: some View {
        HStack(spacing: 8) {
            FormatButtonGroup(store: store)
            Divider()
            HeadingMenu(store: store)
            Divider()
            ListButtonGroup(store: store)
            Divider()
            InsertButtonGroup(store: store)
        }
    }
}

// 格式按钮组：FormatButtonGroup
struct FormatButtonGroup: View {
    let store: StoreOf<EditorFeature>
    
    var body: some View {
        ControlGroup {
            ToolbarButton(
                title: "加粗",
                icon: "bold",
                shortcut: "⌘B"
            ) {
                store.send(.formatBold)
            }
            
            ToolbarButton(
                title: "斜体",
                icon: "italic",
                shortcut: "⌘I"
            ) {
                store.send(.formatItalic)
            }
            
            ToolbarButton(
                title: "行内代码",
                icon: "chevron.left.forwardslash.chevron.right",
                shortcut: "⌘E"
            ) {
                store.send(.formatInlineCode)
            }
        }
    }
}

// 标题下拉菜单：HeadingMenu
struct HeadingMenu: View {
    let store: StoreOf<EditorFeature>
    
    var body: some View {
        Menu {
            Button("标题 1", systemImage: "h1.square") {
                store.send(.insertHeading1)
            }
            .keyboardShortcut("1", modifiers: .command)
            
            Button("标题 2", systemImage: "h2.square") {
                store.send(.insertHeading2)
            }
            .keyboardShortcut("2", modifiers: .command)
            
            Button("标题 3", systemImage: "h3.square") {
                store.send(.insertHeading3)
            }
            .keyboardShortcut("3", modifiers: .command)
        } label: {
            Label("标题", systemImage: "textformat.size")
                .labelStyle(.iconOnly)
        } primaryAction: {
            store.send(.insertHeading1)  // 点击主体时插入 H1
        }
        .help("插入标题")
    }
}

// 可复用按钮组件：ToolbarButton
struct ToolbarButton: View {
    let title: String
    let icon: String
    let shortcut: String
    let action: () -> Void
    
    var body: some View {
        Button(action: action) {
            Label(title, systemImage: icon)
                .labelStyle(.iconOnly)
        }
        .help("\(title) \(shortcut)")
    }
}
```

---

### 4.3 状态管理（TCA）

#### State 扩展（无需修改 EditorFeature.State）

```swift
// ✅ 复用现有 State，通过计算属性提供工具栏状态
extension EditorFeature.State {
    // 工具栏按钮可用性
    var canFormat: Bool {
        note != nil  // 有笔记时才能格式化
    }
    
    var canToggleStar: Bool {
        note != nil  // 有笔记时才能切换星标
    }
    
    var isStarred: Bool {
        note?.isStarred ?? false
    }
}
```

#### Action 复用（无需新增 Action）

```swift
// ✅ 工具栏按钮直接使用已有 Action
// EditorFeature.Action 已包含：
// - formatBold
// - formatItalic
// - formatInlineCode
// - insertHeading1/2/3
// - insertUnorderedList
// - insertOrderedList
// - insertTaskList
// - insertLink
// - insertCodeBlock
// - toggleStar
```

#### Reducer 无需修改

```swift
// ✅ Reducer 逻辑已完整实现
// 工具栏按钮触发 Action → Reducer 处理 → 插入 Markdown
```

---

### 4.4 SwiftUI 4.0 特性应用

| SwiftUI 4.0 特性 | 应用场景 | 优势 |
|------------------|---------|------|
| **Toolbar API** | 工具栏位置 | 系统集成、自动布局 |
| **ViewThatFits** | 响应式布局 | 自动适配窗口大小 |
| **ControlGroup** | 按钮分组 | macOS 原生风格 |
| **Menu + primaryAction** | 标题下拉菜单 | 点击 = H1，长按 = 菜单 |
| **SF Symbols 4.0** | 图标系统 | 自动变体、分层渲染 |
| **.help()** | 工具提示 | 多行富文本提示 |
| **@Bindable** | 状态绑定 | 简化 Store 绑定 |
| **@Environment** | 系统适配 | 响应深色模式、字体大小 |

---

## 5. 交互设计

### 5.1 基础交互

#### 按钮点击

```
用户点击"加粗"按钮
  ↓
发送 Action: .formatBold
  ↓
Reducer 转发: .insertMarkdown(.bold)
  ↓
在光标位置插入: "****"
  ↓
光标移动到中间（自动选中）
```

#### 下拉菜单

```
用户点击"标题"按钮
  ↓
【快速点击】插入 H1（primaryAction）
【长按/右键】显示下拉菜单
  ↓
选择 H1/H2/H3
  ↓
插入对应标题标记
```

---

### 5.2 状态反馈

| 状态 | 视觉表现 | 说明 |
|-----|---------|------|
| **默认** | 图标灰色，背景透明 | 按钮可用 |
| **Hover** | 背景浅色高亮 | 鼠标悬停 |
| **Active** | 背景深色高亮 | 鼠标按下 |
| **Disabled** | 图标灰色 50% 透明度 | 按钮不可用 |
| **Focused** | 蓝色外框 | 键盘导航焦点 |

---

### 5.3 工具提示

```swift
Button("加粗", systemImage: "bold") {
    store.send(.formatBold)
}
.help("加粗 ⌘B\n\n在光标位置插入加粗标记 **文本**")
```

**显示规则**：
- **延迟**：Hover 1 秒后显示
- **位置**：按钮下方居中
- **内容**：功能名 + 快捷键 + 简短说明
- **样式**：系统标准工具提示（黄色背景）

---

### 5.4 键盘导航

```
Tab → 聚焦第一个按钮（加粗）
Space/Enter → 触发按钮
Tab → 下一个按钮
Shift+Tab → 上一个按钮
Esc → 退出工具栏焦点
```

---

### 5.5 辅助功能

#### VoiceOver 支持

```swift
Button("加粗", systemImage: "bold") {
    store.send(.formatBold)
}
.accessibilityLabel("加粗")
.accessibilityHint("在光标位置插入加粗格式，快捷键 Command B")
.accessibilityAddTraits(.isButton)
```

**朗读顺序**：
1. 功能名称（"加粗"）
2. 按钮类型（"按钮"）
3. 状态（"已启用" / "已禁用"）
4. 提示（"在光标位置插入加粗格式"）

#### Dynamic Type 支持

```swift
@Environment(\.dynamicTypeSize) var typeSize

var body: some View {
    HStack(spacing: typeSize >= .accessibility1 ? 12 : 8) {
        // 大字体模式：增加间距
    }
}
```

---

## 6. 视觉设计

### 6.1 布局规范

#### 完整模式（宽度 > 1000px）

```
┌────────────────────────────────────────────────────────────────┐
│  [𝐁] [𝐼] [`]  │  [H▼]  │  [•] [1.] [☑]  │  [🔗] [{…}]  │  [⭐] │
└────────────────────────────────────────────────────────────────┘
   格式组          标题    列表组           插入组        笔记
```

#### 紧凑模式（宽度 600-1000px）

```
┌──────────────────────────────────────────────────────┐
│  [𝐁] [𝐼] [`]  │  [H▼]  │  [•] [1.]  │  [更多▼]      │
└──────────────────────────────────────────────────────┘
```

#### 最小模式（宽度 < 600px）

```
┌─────────────────────┐
│  [格式▼]  [更多▼]   │
└─────────────────────┘
```

---

### 6.2 尺寸规范

| 元素 | 尺寸 | 说明 |
|-----|------|------|
| **按钮高度** | 28pt | 符合 macOS 标准 |
| **按钮最小宽度** | 32pt | 保证点击区域 |
| **图标大小** | 16pt | 清晰可见 |
| **按钮间距** | 8pt | 组内间距 |
| **分组间距** | 12pt | 分隔符两侧 |
| **工具栏内边距** | 8pt | 左右边距 |

---

### 6.3 颜色规范

#### Light Mode

| 元素 | 颜色 | Hex |
|-----|------|-----|
| **图标（默认）** | Secondary | `#8E8E93` |
| **图标（Hover）** | Primary | `#000000` |
| **背景（Hover）** | Fill | `#F2F2F7` |
| **背景（Active）** | Secondary Fill | `#E5E5EA` |
| **分隔符** | Separator | `#C6C6C8` |

#### Dark Mode

| 元素 | 颜色 | Hex |
|-----|------|-----|
| **图标（默认）** | Secondary | `#98989D` |
| **图标（Hover）** | Primary | `#FFFFFF` |
| **背景（Hover）** | Fill | `#3A3A3C` |
| **背景（Active）** | Secondary Fill | `#48484A` |
| **分隔符** | Separator | `#38383A` |

---

### 6.4 字体规范

| 用途 | 字体 | 大小 | 粗细 |
|-----|------|------|------|
| **按钮图标** | SF Symbols | 16pt | Regular |
| **工具提示** | SF Pro Text | 12pt | Regular |
| **菜单项** | SF Pro Text | 13pt | Regular |

---

### 6.5 图标映射

| 功能 | SF Symbol | 说明 |
|-----|-----------|------|
| **加粗** | `bold` | 标准符号 |
| **斜体** | `italic` | 标准符号 |
| **行内代码** | `chevron.left.forwardslash.chevron.right` | `</>` 符号 |
| **标题** | `textformat.size` | 文本大小 |
| **标题 1** | `h1.square` | H1 方形 |
| **标题 2** | `h2.square` | H2 方形 |
| **标题 3** | `h3.square` | H3 方形 |
| **无序列表** | `list.bullet` | 无序列表 |
| **有序列表** | `list.number` | 有序列表 |
| **任务列表** | `checklist` | 复选框 |
| **链接** | `link` | 链接符号 |
| **代码块** | `curlybraces` | 大括号 |
| **星标（未选中）** | `star` | 空心星 |
| **星标（已选中）** | `star.fill` | 实心星 |

---

## 7. 响应式设计

### 7.1 窗口宽度适配

```swift
struct MarkdownToolbar: View {
    let store: StoreOf<EditorFeature>
    
    var body: some View {
        ViewThatFits {
            // 优先级 1: 完整模式（> 1000px）
            fullToolbar
            
            // 优先级 2: 标准模式（800-1000px）
            standardToolbar
            
            // 优先级 3: 紧凑模式（600-800px）
            compactToolbar
            
            // 优先级 4: 最小模式（< 600px）
            minimalToolbar
        }
    }
    
    private var fullToolbar: some View {
        HStack(spacing: 8) {
            FormatButtonGroup(store: store)      // 格式（3 个）
            Divider()
            HeadingMenu(store: store)            // 标题（菜单）
            Divider()
            ListButtonGroup(store: store)        // 列表（3 个）
            Divider()
            InsertButtonGroup(store: store)      // 插入（2 个）
            Divider()
            StarButton(store: store)             // 星标（1 个）
        }
    }
    
    private var standardToolbar: some View {
        HStack(spacing: 8) {
            FormatButtonGroup(store: store)      // 格式（3 个）
            Divider()
            HeadingMenu(store: store)            // 标题（菜单）
            Divider()
            BasicListButtons(store: store)       // 列表（2 个：无序、有序）
            Divider()
            Menu("更多") {
                TaskListButton(store: store)     // 任务列表
                Divider()
                InsertButtonGroup(store: store)  // 插入（2 个）
                Divider()
                StarButton(store: store)         // 星标
            }
        }
    }
    
    private var compactToolbar: some View {
        HStack(spacing: 8) {
            FormatButtonGroup(store: store)      // 格式（3 个）
            Divider()
            HeadingMenu(store: store)            // 标题（菜单）
            Divider()
            Button("更多", systemImage: "ellipsis.circle") {
                // 显示更多菜单
            }
        }
    }
    
    private var minimalToolbar: some View {
        HStack(spacing: 8) {
            Menu("格式", systemImage: "textformat") {
                FormatSection(store: store)
                HeadingSection(store: store)
                ListSection(store: store)
                InsertSection(store: store)
            }
        }
    }
}
```

---

### 7.2 视图模式适配

| 视图模式 | 工具栏显示 | 说明 |
|---------|-----------|------|
| **仅编辑** | 显示完整工具栏 | 编辑器上方 |
| **仅预览** | 隐藏工具栏 | 预览模式不需要编辑 |
| **分屏** | 仅在编辑器侧显示 | 左侧编辑器显示，右侧预览隐藏 |

```swift
struct NoteEditorView: View {
    @Bindable var store: StoreOf<EditorFeature>
    
    var body: some View {
        Group {
            switch store.viewMode {
            case .editOnly:
                TextEditor(text: $store.content)
                    .toolbar {
                        MarkdownToolbar(store: store)  // ✅ 显示工具栏
                    }
                
            case .previewOnly:
                MarkdownPreview(content: store.content)
                // ❌ 不显示工具栏
                
            case .split:
                HSplitView {
                    TextEditor(text: $store.content)
                        .toolbar {
                            MarkdownToolbar(store: store)  // ✅ 编辑器侧显示
                        }
                    
                    MarkdownPreview(content: store.content)
                    // ❌ 预览侧不显示
                }
            }
        }
    }
}
```

---

### 7.3 字体大小适配

```swift
@Environment(\.dynamicTypeSize) var typeSize

var buttonSpacing: CGFloat {
    switch typeSize {
    case .xSmall, .small, .medium:
        return 8
    case .large, .xLarge, .xxLarge:
        return 10
    case .xxxLarge, .accessibility1, .accessibility2:
        return 12
    default:
        return 14
    }
}

var iconSize: Font {
    switch typeSize {
    case .xSmall, .small:
        return .system(size: 14)
    case .medium, .large:
        return .system(size: 16)
    case .xLarge, .xxLarge, .xxxLarge:
        return .system(size: 18)
    default:
        return .system(size: 20)
    }
}
```

---

## 8. 开发计划

### 8.1 开发阶段

#### 阶段 1: 基础工具栏（2-3 小时）⭐

**任务清单**：

1. ✅ 创建 `MarkdownToolbar.swift`
2. ✅ 实现 `ToolbarButton` 可复用组件
3. ✅ 实现 `FormatButtonGroup`（加粗、斜体、行内代码）
4. ✅ 实现 `ListButtonGroup`（无序、有序列表）
5. ✅ 实现 `InsertButtonGroup`（链接、代码块）
6. ✅ 集成到 `NoteEditorView`
7. ✅ 测试所有按钮功能

**验收标准**：
- 所有 P0 按钮可正常工作
- 点击按钮正确插入 Markdown 语法
- 工具提示显示正确
- 按钮状态（启用/禁用）正确

---

#### 阶段 2: 标题下拉菜单（1 小时）

**任务清单**：

1. ✅ 实现 `HeadingMenu` 组件
2. ✅ 配置 `primaryAction`（点击 = H1）
3. ✅ 测试下拉菜单交互
4. ✅ 验证快捷键仍然工作

**验收标准**：
- 点击主体插入 H1
- 长按/右键显示 H1/H2/H3 菜单
- 菜单项显示快捷键提示
- 选择菜单项正确插入标题

---

#### 阶段 3: 响应式布局（1.5 小时）

**任务清单**：

1. ✅ 实现 `ViewThatFits` 响应式布局
2. ✅ 创建 `standardToolbar`（标准模式）
3. ✅ 创建 `compactToolbar`（紧凑模式）
4. ✅ 创建 `minimalToolbar`（最小模式）
5. ✅ 测试窗口大小变化
6. ✅ 验证"更多"菜单功能

**验收标准**：
- 窗口大小变化时自动切换布局
- 所有功能在不同布局下均可用
- 过渡动画流畅自然

---

#### 阶段 4: 视图模式适配（0.5 小时）

**任务清单**：

1. ✅ 在仅编辑模式显示工具栏
2. ✅ 在仅预览模式隐藏工具栏
3. ✅ 在分屏模式仅在编辑器侧显示
4. ✅ 测试视图模式切换

**验收标准**：
- 切换视图模式时工具栏显示/隐藏正确
- 分屏模式下只有编辑器侧显示工具栏

---

#### 阶段 5: 单元测试（1 小时）

**任务清单**：

1. ✅ 测试工具栏按钮触发 Action
2. ✅ 测试按钮禁用状态逻辑
3. ✅ 测试响应式布局切换
4. ✅ 测试视图模式适配

**验收标准**：
- 测试覆盖率 ≥ 80%
- 所有核心功能有对应测试
- 边界情况有测试覆盖

---

#### 阶段 6: 优化与完善（1 小时）

**任务清单**：

1. ✅ 优化按钮样式和间距
2. ✅ 完善工具提示文案
3. ✅ 添加辅助功能支持
4. ✅ 性能优化（减少重绘）
5. ✅ 用户测试与反馈收集

**验收标准**：
- 视觉效果符合设计规范
- 工具提示清晰易懂
- VoiceOver 支持完整
- 无明显性能问题

---

### 8.2 总体时间估算

| 阶段 | 预计时间 | 优先级 |
|-----|---------|--------|
| **阶段 1**（基础工具栏） | 2-3 小时 | ⭐⭐⭐ |
| **阶段 2**（标题菜单） | 1 小时 | ⭐⭐⭐ |
| **阶段 3**（响应式布局） | 1.5 小时 | ⭐⭐ |
| **阶段 4**（视图模式适配） | 0.5 小时 | ⭐⭐ |
| **阶段 5**（单元测试） | 1 小时 | ⭐⭐ |
| **阶段 6**（优化完善） | 1 小时 | ⭐ |

**总计**：7-8 小时

---

### 8.3 里程碑

| 里程碑 | 日期 | 交付物 |
|--------|------|--------|
| **M1: 基础工具栏完成** | 待定 | P0 功能可用，基础测试通过 |
| **M2: 标题菜单完成** | 待定 | 下拉菜单可用，交互流畅 |
| **M3: 响应式布局完成** | 待定 | 自动适配窗口大小 |
| **M4: 正式发布** | 待定 | 优化完成，文档完整 |

---

## 9. 测试计划

### 9.1 单元测试

**测试范围**：工具栏按钮触发 Action

**测试用例**：

```swift
final class MarkdownToolbarTests: XCTestCase {
    @MainActor
    func testFormatBoldButton() async {
        let store = TestStore(initialState: EditorFeature.State()) {
            EditorFeature()
        }
        
        // 模拟点击加粗按钮
        await store.send(.formatBold)
        await store.receive(.insertMarkdown(.bold))
    }
    
    @MainActor
    func testButtonDisabledWithoutNote() async {
        let store = TestStore(initialState: EditorFeature.State()) {
            EditorFeature()
        }
        
        // 验证无笔记时按钮应禁用
        XCTAssertFalse(store.state.canFormat)
    }
    
    @MainActor
    func testStarButtonState() async {
        var initialState = EditorFeature.State()
        initialState.note = Note(noteId: "test", title: "Test", content: "")
        
        let store = TestStore(initialState: initialState) {
            EditorFeature()
        }
        
        // 验证星标按钮状态
        XCTAssertTrue(store.state.canToggleStar)
        XCTAssertFalse(store.state.isStarred)
    }
}
```

---

### 9.2 UI 测试

**测试场景**：

1. ✅ 工具栏按钮显示正确
2. ✅ 点击按钮插入正确的 Markdown 语法
3. ✅ Hover 显示工具提示
4. ✅ 禁用按钮不可点击
5. ✅ 窗口大小变化时布局自动切换
6. ✅ 视图模式切换时工具栏显示/隐藏

---

### 9.3 手动测试

**测试清单**：

#### 功能测试

- [ ] 点击加粗按钮，验证插入 `****`
- [ ] 点击斜体按钮，验证插入 `**`
- [ ] 点击行内代码按钮，验证插入 `` `` ``
- [ ] 点击标题菜单主体，验证插入 `# `
- [ ] 展开标题菜单，选择 H2，验证插入 `## `
- [ ] 点击无序列表，验证插入 `\n- `
- [ ] 点击有序列表，验证插入 `\n1. `
- [ ] 点击任务列表，验证插入 `\n- [ ] `
- [ ] 点击链接，验证插入 `[]()`
- [ ] 点击代码块，验证插入 `` ```\n\n``` ``
- [ ] 点击星标，验证切换状态

#### 交互测试

- [ ] Hover 按钮显示工具提示
- [ ] 工具提示内容正确（功能名 + 快捷键）
- [ ] Tab 键导航焦点
- [ ] Space 键触发按钮
- [ ] 快捷键仍然工作（⌘B、⌘I 等）

#### 状态测试

- [ ] 无笔记时按钮禁用
- [ ] 禁用按钮显示灰色
- [ ] 禁用按钮不可点击
- [ ] 星标按钮显示正确图标（空心/实心）

#### 响应式测试

- [ ] 窗口宽度 > 1000px：显示完整工具栏
- [ ] 窗口宽度 800-1000px：显示标准工具栏 + "更多"菜单
- [ ] 窗口宽度 600-800px：显示紧凑工具栏
- [ ] 窗口宽度 < 600px：显示最小工具栏
- [ ] 过渡动画流畅

#### 视图模式测试

- [ ] 仅编辑模式：显示工具栏
- [ ] 仅预览模式：隐藏工具栏
- [ ] 分屏模式：仅编辑器侧显示工具栏

#### 辅助功能测试

- [ ] VoiceOver 正确朗读按钮名称
- [ ] VoiceOver 朗读按钮状态（启用/禁用）
- [ ] 增加字体大小后布局正确
- [ ] 高对比度模式下图标清晰

---

### 9.4 性能测试

**测试指标**：

| 指标 | 目标 | 说明 |
|-----|------|------|
| **首次渲染** | < 16ms | 60fps 流畅度 |
| **按钮点击响应** | < 50ms | 即时反馈 |
| **布局切换动画** | < 300ms | 流畅过渡 |
| **内存占用** | < 5MB | 工具栏组件 |

---

## 10. 成功指标

### 10.1 功能指标

- ✅ 所有 P0 功能正常工作（100%）
- ✅ 所有 P1 功能正常工作（100%）
- ✅ 测试覆盖率 ≥ 80%
- ✅ 响应式布局在所有窗口大小下正确工作

### 10.2 性能指标

- ✅ 工具栏渲染时间 < 16ms（60fps）
- ✅ 按钮点击响应 < 50ms
- ✅ 布局切换动画 < 300ms
- ✅ 内存占用 < 5MB

### 10.3 用户体验指标

- ✅ 用户满意度 ≥ 4.0/5.0
- ✅ 功能发现率 ≥ 90%（用户能找到需要的功能）
- ✅ 新手用户首次使用成功率 ≥ 85%
- ✅ 快捷键使用率 ≥ 30%（活跃用户仍使用快捷键）

### 10.4 质量指标

- ✅ 0 个 P0/P1 Bug
- ✅ VoiceOver 支持完整（100% 功能可访问）
- ✅ 深色模式无视觉问题
- ✅ 高对比度模式无可读性问题

---

## 11. 风险与挑战

### 11.1 技术风险

| 风险 | 影响 | 缓解措施 |
|-----|------|---------|
| **SwiftUI Toolbar 位置问题** | 中 | 使用 `.automatic` placement 让系统决定 |
| **ViewThatFits 性能** | 低 | 限制候选布局数量（≤ 4 个） |
| **按钮状态同步** | 中 | 使用 TCA State 作为单一数据源 |
| **响应式布局闪烁** | 低 | 使用 `.animation()` 平滑过渡 |

### 11.2 用户体验风险

| 风险 | 影响 | 缓解措施 |
|-----|------|---------|
| **工具栏占用空间** | 中 | 支持隐藏工具栏（快捷键：⌥⌘T） |
| **按钮过多导致混乱** | 中 | 响应式设计，窄屏时收起部分按钮 |
| **新手用户不知道工具栏存在** | 高 | 首次启动时高亮提示工具栏 |

---

## 12. 附录

### 12.1 参考资料

- [macOS Human Interface Guidelines - Toolbars](https://developer.apple.com/design/human-interface-guidelines/toolbars)
- [SwiftUI Toolbar Documentation](https://developer.apple.com/documentation/swiftui/view/toolbar(content:)-5w0tj)
- [SF Symbols 4.0 Documentation](https://developer.apple.com/sf-symbols/)
- [The Composable Architecture Documentation](https://pointfreeco.github.io/swift-composable-architecture/)

### 12.2 竞品分析

| 应用 | 工具栏位置 | 按钮数量 | 特色功能 |
|-----|-----------|---------|---------|
| **Typora** | 顶部浮动 | 12 个 | 悬浮工具栏，自动隐藏 |
| **Notion** | 顶部固定 | 15 个 | 分组清晰，图标美观 |
| **Bear** | 顶部固定 | 8 个 | 极简设计，只保留核心功能 |
| **Nota4** | 顶部固定 | 12 个（P0+P1） | SwiftUI 原生，响应式设计 |

### 12.3 快捷键一览表

| 功能 | 快捷键 | 说明 |
|-----|--------|------|
| **加粗** | ⌘B | 插入 `****` |
| **斜体** | ⌘I | 插入 `**` |
| **行内代码** | ⌘E | 插入 `` `` `` |
| **标题 1** | ⌘1 | 插入 `# ` |
| **标题 2** | ⌘2 | 插入 `## ` |
| **标题 3** | ⌘3 | 插入 `### ` |
| **无序列表** | ⌘L | 插入 `\n- ` |
| **有序列表** | ⇧⌘L | 插入 `\n1. ` |
| **任务列表** | ⌥⌘L | 插入 `\n- [ ] ` |
| **链接** | ⌘K | 插入 `[]()` |
| **代码块** | ⇧⌘K | 插入 `` ```\n\n``` `` |
| **切换星标** | ⌘D | 切换星标状态 |
| **隐藏工具栏** | ⌥⌘T | 切换工具栏显示/隐藏 |

---

**最后更新**: 2025-11-16  
**下一步**: 等待批准后开始阶段 1 开发








